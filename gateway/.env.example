# =============================================================================
# SITE IDENTIFICATION
# =============================================================================

# Unique name for this deployment (used for DNS service discovery)
# Gateway will be accessible as: ${SITE_NAME}.zmesh
SITE_NAME=mysite

# =============================================================================
# ZEROTIER CONFIGURATION
# =============================================================================

# ZeroTier Network ID - Get this from your ZTNet controller or my.zerotier.com
NETWORK_ID=abc123def456

# Gateway will automatically get a ZeroTier IP (e.g., 10.121.15.217)
# Clients access via DNS: mysite.zmesh → 10.121.15.217

# Reverse Proxy Configuration
ENDPOINTS=wordpress:80

# Optional: Multiple endpoints
# ENDPOINTS=wordpress:80,phpmyadmin:80
# DOMAINS=wp.zmesh,pma.zmesh

# SSL Configuration (keep false for ZeroTier-only)
SSL_ENABLED=false
DOMAIN=mysite.zmesh
EMAIL=admin@example.com

# Database Configuration
DB_NAME=wordpress
DB_USER=wpuser
DB_PASSWORD=change_me_to_secure_password
